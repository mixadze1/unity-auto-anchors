# Default configuration sample

Create a project configuration with `Tools/Auto Anchor/Create Configuration`. Unity creates an editable `AutoAnchorConfig` asset in `Assets` with the default `Shift+[` and `Shift+]` shortcuts.
